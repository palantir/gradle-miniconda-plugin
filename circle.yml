machine:
  java:
    version: oraclejdk7
  environment:
    TERM: dumb

general:
  artifacts:
    - build/libs

test:
  override:
    - ./gradlew build
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - cp build/test-results/*.xml $CIRCLE_TEST_REPORTS/junit/ 
